<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="si.um.feri.demo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".DemoApplication">

        <activity android:name=".PregledActivity" android:label="Pregled"/>
        <activity android:name=".SeznamActivity" android:label="Seznam"/>
        <activity android:name=".FancySeznamActivity" android:label="Seznam" android:exported="true"/>
        <activity android:name=".VnosActivity" android:label="Vnos podatkov">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>